# Route Codex through local proxy to OpenRouter (port 8009)
# The proxy handles the WebSocket issue: accepts WS upgrade, then
# immediately closes it so Codex falls back to HTTP with no retries.
openai_base_url = "http://127.0.0.1:8009"

model = "qwen/qwen3.6-plus"
